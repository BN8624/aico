# CONTEXT_NOTES

- 2026-07-06: P3W completion review found no blocker. The actual single-call artifacts prove one provider call, counters 0 -> 1, retry/reserve/fallback/second-call all zero/false, raw output not saved, masked summary saved, and artifact safety scan pass.
- 2026-07-06: P3X entry is YES only for negative safety tests / bad-input blocking proof. P3X should not run another live call by default; it should use fake provider, blocked paths, and artifact injection to prove bad inputs cannot cause second call, retry, reserve/fallback, raw leak, or worker authority expansion.
- 2026-07-06: P3W Gemma 4 31B IT actual single-call run completed with the existing runner. Official Google documentation confirmed the Gemma 4 31B family, and the actual Gemini API call validated model id `gemma-4-31b-it` with provider `google_gemini`, key_slot `worker_1`, and one process-local mapping from `GOOGLE_API_KEY_1` to `AICO_WORKER_1_API_KEY`.
- 2026-07-06: The P3W actual run made exactly one provider call. Counts were `actual_provider_call_count=1`, `call_model_count_after=1`, and `model_call_count_after=1`; retry, reserve, fallback, and second call stayed at zero/false.
- 2026-07-06: The P3W actual run did not dump `.env`, did not persist raw key/env/provider response/token usage/raw output, did not rotate 11 keys, and stored only masked summary plus safe artifacts under ignored `runs/p3w_20260706T123731Z`.
- 2026-07-06: P3W success proves only the provider boundary one-call path. It does not authorize worker orchestration, shell/web/repo/GitHub access, retries, reserve/fallback, second calls, raw output persistence, or broader live operation.
- 2026-07-06: P3W Gemma 4 31B IT opt-in actual run was attempted with the existing runner, but it safe-blocked before provider activation. `.env` exists, but required P3W opt-in values were absent, the exact Gemma 4 31B IT safe model id was not confirmed from repo registry or `.env` opt-in config, and no known non-reserve `key_registry` slot variable was found in `.env`.
- 2026-07-06: The P3W Gemma attempt did not dump `.env`, did not print or persist any raw key, did not load 11 keys, did not rotate keys, did not read reserve/fallback keys, and did not perform a provider call. Counts stayed at `actual_provider_call_count=0`, `call_model_count_after=0`, and `model_call_count_after=0`.
- 2026-07-06: A later P3W actual call still requires exact process opt-in values, one non-reserve key_slot known to `key_registry`, and an exact safe model id for Gemma 4 31B IT. AICO must not infer model ids or map arbitrary `.env` variables to key slots.
- 2026-07-05: `AICO_MASTER_CANON.md` has highest priority, followed by `AICO_V0_CANON.md`.
- 2026-07-05: v0 must stay fully offline with zero API calls and zero LLM calls.
- 2026-07-05: `semantic_preflight`, repair loops, worker file edits, and worker shell execution are explicitly out of scope.
- 2026-07-05: Dry-run scenarios will be deterministic fixtures instead of provider-backed execution.
- 2026-07-05: Failure paths must still attempt `ceo_report.md`; if report generation fails, `REPORT_ERROR` must be logged.
- 2026-07-05: Implemented the harness with a flat `aico_v0` Python package and standard-library-only runtime code.
- 2026-07-05: Scenario fixtures cover pass, conditional, fail, needs_decision, budget_exceeded, and mid_flight_failure; config_error is handled before fixture execution.
- 2026-07-05: `runs/` is ignored so default harness output does not become source-controlled state.
- 2026-07-05: P2 review found no current external API/LLM/network/dashboard/Issue path in `aico_v0`, but P3 remains blocked by missing Required Test coverage and missing `REPORT_ERROR` fallback when report generation fails.
- 2026-07-05: P2 hardening scope is limited to v0 dry-run harness behavior and tests; P3 API worker, actual API/LLM calls, semantic preflight, repair loops, network, web dashboard, repo clone, and GitHub Issue integration remain forbidden.
- 2026-07-05: P2 hardening added direct Runtime Tests mapping for 42 V0 Canon tests and implemented `REPORT_ERROR` fallback for `ceo_report.md` write failures.
- 2026-07-05: Created `AICO_P3_CANON.md` as a documentation-only extraction for P3 API Worker 4 scope; no P3 implementation, API client, provider connection, real key use, or model call was added.
- 2026-07-05: P3 Canon review found P3 implementation should remain blocked until document priority, retry/reserve budget semantics, and malformed response artifact rules are clarified.
- 2026-07-05: Corrected `AICO_P3_CANON.md` priority, retry/reserve budget semantics, malformed provider response artifact rules, and P3 Required Tests; no P3 code was implemented.
- 2026-07-05: P3A implementation scope is fake-provider only. It must not add real provider adapters, API key loading, network calls, semantic_preflight, repair loop, worker file edits, worker shell execution, GitHub Issue integration, dashboard, or CLI agent orchestration.
- 2026-07-05: Implemented P3A with fake provider only, key_slot logging, fake model call accounting, reserve recovery for worker MODEL_ERROR, failure type separation, masking rules, and dedicated P3A tests. No real API, LLM, key loading, or network path was added.
- 2026-07-05: P3A completion review found no blocking issues for P3B entry. P3B should begin with provider adapter boundary design, response normalization, token accounting ownership, retry ownership, and key-loading policy before any live provider calls.
- 2026-07-05: Implemented P3B provider boundary skeleton with shared provider interface, disabled real provider skeleton, key_slot/env-var-name registry, response normalization, and boundary tests. No real API call, LLM call, key value loading, provider SDK import, or network path was added.
- 2026-07-05: P3B completion review blocks P3C entry until the invalid key_slot `ProviderResult(error=...)` branch is fixed, direct tests are added for invalid key_slot and raw_key_value disabled access, and ProviderResult construction policy is tightened before real provider work.
- 2026-07-05: Fixed P3B blockers by removing the invalid key_slot `ProviderResult(error=...)` path, adding direct invalid key_slot/raw_key_value/ProviderResult safety tests, and hardening ProviderResult masking and validation. P3C entry is now YES for guarded disabled-by-default provider work only.
- 2026-07-05: Implemented P3C guarded disabled-by-default provider work with `AICO_ENABLE_REAL_PROVIDER` config semantics, `DisabledTransport` default, injectable `FakeTransport`, response normalization, and guard tests. No real API call, LLM call, key value read, provider SDK import, or network path was added.
- 2026-07-05: P3C completion review found no blocking issues for P3D policy entry. P3D should start with live-call gate policy documentation only; real key loading, provider SDK import, network transport, and live API calls remain forbidden until explicitly authorized.
- 2026-07-05: P3D is live-call gate documentation only, not live-call permission. Actual provider activation requires a later explicit phase and explicit user approval. Default `pytest -q` must remain offline-only with fake, stub, or disabled transport.
- 2026-07-05: P3D completion review sets P3E entry to NO. Before P3E, live-call gate failures need canonical `failure_type` mapping, the provider allowlist must be decided, P3E scope must be clarified, and artifact safety scan test requirements must be finalized.
- 2026-07-05: P3D policy fix sets P3E scope to activation preparation only, not actual live activation. First live smoke is deferred to P3F or a later explicit approval phase. Provider allowlist default is empty. Artifact safety scan is a required blocker before any live smoke.
- 2026-07-05: Implemented P3E activation preparation with approval schema, live gate validator, empty-default provider allowlist, artifact safety scanner, offline-only marker policy, and tests. This is still not live activation; provider allowlist stays empty by default, artifact safety scan is required before live smoke, and first live smoke remains deferred to P3F or a later explicit approval phase.
- 2026-07-05: P3E completion review sets P3F entry to NO. Before P3F, `LiveApproval` fields must be scanned for raw secret-like values and direct tests must prove approval-object secret ingress becomes SECURITY_BLOCKED.
- 2026-07-05: Fixed P3E blocker by adding recursive LiveApproval secret guard and direct tests. P3F entry is YES only for first live smoke policy/preparation. Actual live smoke, real keys, provider SDK imports, and network calls remain forbidden until a later explicit approval phase.
- 2026-07-05: P3F is first live smoke policy/preparation only, not live smoke execution. Actual first live smoke is deferred to P3G or a later explicit approval phase. The policy limits future smoke to one provider candidate, one key_slot, one model call, retry 0, no reserve, no raw output, and artifact safety scans before and after the call.
- 2026-07-05: P3F completion review sets P3G entry to NO. Before P3G, fix P3F document priority so the P3F policy outranks P3D for first-live-smoke-specific rules, and add missing failure/stop mappings for unknown endpoint and artifact safety scan failure.
- 2026-07-05: P3F policy fix adds P3F self-priority above P3D. Unknown endpoint requested maps to SECURITY_BLOCKED. Artifact safety scan missing maps to CONFIG_ERROR. Artifact safety scan failed maps to SECURITY_BLOCKED. P3G entry YES is only for live smoke implementation skeleton or policy/preparation, not actual live smoke approval.
- 2026-07-05: P3G implements first live smoke skeleton only, not actual live smoke. It adds safe approval/gate validation plus `live_smoke_result` and `artifact_safety_report` schema helpers. Actual live smoke remains forbidden until a later explicit approval phase.
- 2026-07-05: P3G completion review sets P3H entry to YES only for live smoke approval package or policy/preparation. Actual live smoke remains forbidden. Before real artifact writes, harden or centrally gate `live_smoke_result` values such as model call count, retry count, and reserve usage.
- 2026-07-05: P3H documents the first live smoke approval package only. Actual live smoke is still deferred to P3I or a later explicit approval phase. The approval package centralizes provider/model/key_slot candidate handling, fixed budget, raw-output ban, artifact safety requirements, stop conditions, and rollback rules.
- 2026-07-05: P3H policy fix adds `P3H_LIVE_SMOKE_APPROVAL_PACKAGE.md` to its own Document Priority and makes P3H/P3F conflict handling explicit. Master/P3 Canon remain highest priority. P3I entry YES after this fix is not live smoke approval; actual live smoke still requires a later explicit approval phase.
- 2026-07-05: P3I is final preflight / approval review documentation only, not actual live smoke. Actual live smoke is deferred to P3J or a later explicit approval phase. P3I reviews provider/model/key_slot candidates, allowlist opening conditions, SDK import, key loading, artifact write preflight, artifact safety scan preflight, failure mapping, stop conditions, and rollback procedure before any future first live smoke.
- 2026-07-05: P3I completion review sets P3J entry to YES only for live smoke execution skeleton / artifact write integration preparation. P3J entry is not live smoke approval. Actual live smoke, SDK import, key loading, allowlist opening, and network transport remain deferred until a later explicit approval phase.
- 2026-07-06: P3J implements execution skeleton / artifact write integration only, not actual live smoke. It adds run_dir path guard, safe `live_smoke_result.json` and `artifact_safety_report.json` write helpers, pre/post artifact safety scan skeleton, and disabled runner artifact integration. Actual API/LLM/key/SDK/network/live smoke counts must remain zero after P3J.
- 2026-07-06: P3J completion review sets P3K entry to YES only for live provider activation skeleton / allowlist opening skeleton preparation. P3K entry is not live smoke approval. Actual live smoke, provider SDK import, key loading, provider allowlist activation, network transport, and real API/LLM calls remain deferred until a later explicit approval phase.
- 2026-07-06: P3K implements live provider activation skeleton / allowlist opening skeleton only, not actual provider activation. `google_gemini` is represented as a candidate provider name only. Candidate allowlist state does not allow SDK import, key loading, live calls, endpoint URLs, API calls, LLM calls, network calls, or live smoke. Actual first call remains deferred to P3L or a later explicit approval phase.
- 2026-07-06: P3K completion review sets P3L entry to YES only for SDK/key-loading boundary skeleton preparation. P3L entry is not live smoke approval. Actual provider activation, provider SDK import, real key loading, network transport, API calls, LLM calls, and live smoke remain deferred until a later explicit approval phase.
- 2026-07-06: P3L implements SDK/key-loading boundary skeleton only, not actual SDK import or actual key loading. SDK import boundary location is defined as provider-adapter-internal only, while provider SDK imports remain forbidden.
- 2026-07-06: P3L key loading boundary location is defined as provider adapter internal minimal function only. Env var value reads and raw key reads remain forbidden; key existence checks are boolean skeletons based on injected safe metadata only.
- 2026-07-06: P3L keeps candidate allowlist compatibility closed. A `google_gemini` candidate still does not authorize SDK import, key loading, live calls, API calls, LLM calls, network calls, or live smoke.
- 2026-07-06: Actual first call remains deferred to P3M or a later explicit approval phase. P3L did not perform actual API calls, LLM calls, key usage, provider SDK imports, network calls, or live smoke.
- 2026-07-06: P3M implements final live-call gate implementation skeleton only, not actual live smoke. The final gate composes approval, allowlist, provider candidate, SDK boundary, key loading boundary, key existence, runtime flags, budget, prompt safety, expected output schema, artifact write plan, artifact safety pre-scan, and live-call-disabled gates.
- 2026-07-06: P3M final gate pass means ready_for_review only. `live_call_allowed` remains false and `model_call_count` remains 0 even when all gates pass.
- 2026-07-06: P3M adds a safe `final_live_gate_result.json` schema/write helper. Actual API calls, LLM calls, key reads, provider SDK imports, network calls, provider activation, and live smoke remain forbidden.
- 2026-07-06: Actual first call remains deferred to P3N or a later explicit approval phase.
- 2026-07-06: P3N is dry authorization review documentation only, not actual live smoke. It defines how a future approval package should link to `final_live_gate_result.json` without creating live artifacts or executing a call.
- 2026-07-06: P3N records provider/model/key_slot as dry candidates only. All-gates pass is not live authorization; `live_call_allowed=false` and `model_call_count=0` remain required.
- 2026-07-06: P3N keeps provider allowlist actual activation, SDK import activation, key loading activation, API calls, LLM calls, network calls, and live smoke forbidden. Actual first call is deferred to P3O or a later explicit approval phase.
- 2026-07-06: P3O is execution plan review / explicit approval gate documentation only, not actual live smoke execution.
- 2026-07-06: P3O documents approval phrase intake, the future `approval_package.json` plan, provider/model/key_slot final decision plan, and `final_live_gate_result.json` linkage plan.
- 2026-07-06: P3O keeps `live_call_allowed=false` and `model_call_count=0`. SDK import, key loading, provider allowlist activation, API calls, LLM calls, network calls, and live smoke remain forbidden. Actual first call is deferred to P3P or a later explicit approval phase.
- 2026-07-06: P3P is code activation skeleton / no-call implementation only. It may add parser/schema/linkage/guard helpers, but those helpers must keep `live_call_allowed=false`, `model_call_count=0`, and must not create default/runtime approval artifacts or open SDK/key/provider/API/network/live-smoke execution.
- 2026-07-06: P3P implemented approval phrase parsing, safe approval package schema/write helper, approval phrase hash helper, approval-package/final-gate linkage skeleton, final provider/model/key_slot validators, and disabled activation guards. `approval_package.json` remains opt-in helper output only and is not created by the default/runtime path.
- 2026-07-06: P3P keeps provider allowlist activation, SDK import activation, key loading activation, live call activation, API calls, LLM calls, key reads, network calls, and live smoke at zero. Actual first call remains deferred to P3Q or a later explicit approval phase.
- 2026-07-06: P3Q is provider/key/SDK activation skeleton / no-call integration review only. It connects approval package, final gate result, linkage, artifact safety, and activation guards as a no-call integration path.
- 2026-07-06: P3Q integration pass must still keep `live_call_allowed=false`, `model_call_count=0`, `approval_package.json` default/runtime creation disabled, and `no_call_integration_summary.json` default/runtime creation disabled.
- 2026-07-06: P3Q must not open provider activation, SDK import, key loading, key reads, env value reads, API calls, LLM calls, network calls, or live smoke. Actual first call remains deferred to P3R or a later explicit approval phase.
- 2026-07-06: P3R is live execution boundary skeleton / single-call no-execute dry run only, not actual live smoke.
- 2026-07-06: P3R will add a single-call execution boundary, call attempt state machine, pre-call / blocked-call / post-boundary safety wiring, and rollback plan skeleton while keeping all execution paths no-call.
- 2026-07-06: P3R no-execute dry run passing still means `live_call_allowed=false`, `model_call_count=0`, and no `call_model`, provider transport, SDK import, key loading, env value read, API call, LLM call, network call, or live smoke.
- 2026-07-06: P3R keeps `approval_package.json`, `no_call_integration_summary.json`, and `call_attempt_summary.json` out of the default/runtime path. Helper writes are explicit only and limited to safe test/run directories.
- 2026-07-06: Actual first call remains deferred to P3S or a later explicit approval phase after P3R.
- 2026-07-06: P3S is final pre-live artifact generation skeleton / no-call package assembly only, not actual live smoke.
- 2026-07-06: P3S will assemble approval package, no-call integration summary, call attempt summary, and final live gate result references with run_id, approval_phrase_hash, and artifact reference consistency checks.
- 2026-07-06: P3S package assembly passing still means `live_call_allowed=false`, `model_call_count=0`, `call_model_count=0`, and no SDK import, key loading, provider activation, API call, LLM call, network call, or live smoke.
- 2026-07-06: P3S pre-live artifacts must not be created by default/runtime paths. Explicit helper writes remain run_dir-contained and test-scoped unless a later approval phase changes that.
- 2026-07-06: Actual first call remains deferred to P3T or a later explicit approval phase after P3S.
- 2026-07-06: P3S implemented `pre_live_package_manifest.json` schema/helper, controlled package assembly, run_id/hash/ref consistency checks, pre/post artifact safety wiring, no-call invariant validation, and explicit-only manifest write helper.
- 2026-07-06: P3S targeted tests passed with 56 tests. The default/runtime path still creates no approval package, no-call integration summary, call attempt summary, pre-live package manifest, or live smoke result.
- 2026-07-06: P3T is final live approval packet review / human-confirmation-only no-call phase, not actual live smoke approval or execution.
- 2026-07-06: P3T implemented `final_live_approval_packet.json` and `human_confirmation_checklist.json` safe schemas/helpers, no-call evidence summary, approval phrase reference guard, packet artifact reference guard, and next-step command skeleton guard.
- 2026-07-06: P3T human confirmation statuses remain limited to `pending`, `review_required`, and `not_granted`. Statuses such as `approved`, `granted`, `confirmed`, `execution_approved`, and `live_approved` are blocked.
- 2026-07-06: P3T packet validation keeps `execution_allowed=false`, `live_call_allowed=false`, `model_call_count=0`, `call_model_count=0`, and `raw_output_saved=false` even when the packet is ready for human review.
- 2026-07-06: P3T default/runtime paths do not create approval package, no-call integration summary, call attempt summary, pre-live package manifest, final live approval packet, human confirmation checklist, or live smoke artifacts.
- 2026-07-06: P3T keeps provider activation, SDK import, key loading, key reads, env value reads, API calls, LLM calls, network calls, `call_model`, and live smoke at zero. Actual first call remains deferred to P3U or a later explicit approval phase.
- 2026-07-06: P3U is final explicit approval gate / armed-but-not-fired no-call phase, not actual live smoke execution.
- 2026-07-06: P3U implemented explicit human approval gate, armed-but-not-fired state model, one-shot budget lock, and single-call / no-retry / no-reserve / no-fallback / no-second-call policy locks.
- 2026-07-06: P3U armed state may be `armed_not_fired`, but that is not execution permission. `fired=false`, `execution_allowed=false`, `live_call_allowed=false`, `model_call_count=0`, and `call_model_count=0` remain required.
- 2026-07-06: P3U explicit approval statuses remain limited to `pending`, `review_required`, and `not_granted`; approval-like, success-like, fired, provider-called, SDK-imported, key-loaded, network-called, retry/reserve/fallback/second-call states are blocked.
- 2026-07-06: P3U default/runtime paths do not create approval package, no-call integration summary, call attempt summary, pre-live package manifest, final live approval packet, human confirmation checklist, explicit approval gate, armed state, or live smoke artifacts.
- 2026-07-06: P3U keeps provider activation, SDK import, key loading, key reads, env value reads, API calls, LLM calls, network calls, `call_model`, and live smoke at zero. Actual first call remains deferred to P3V or a later explicit approval phase.
- 2026-07-06: P3V is final live-fire checklist / still-no-call review, not actual live smoke execution.
- 2026-07-06: P3V implemented live-fire checklist, last-stop guard, one-shot fire plan, expected observable artifact schema, fire readiness state model, rollback/no-retry/no-reserve/no-fallback confirmation, artifact reference consistency checks, and still-no-call invariant validation.
- 2026-07-06: P3V `fire_ready=true` and `armed=true` are checklist states only and do not grant execution permission. `fired=false`, `execution_allowed=false`, `live_call_allowed=false`, `model_call_count=0`, and `call_model_count=0` remain required.
- 2026-07-06: P3V default/runtime paths do not create approval package, no-call integration summary, call attempt summary, pre-live package manifest, final live approval packet, human confirmation checklist, explicit approval gate, armed state, live-fire checklist, last-stop guard, one-shot fire plan, expected live artifacts, or live smoke artifacts.
- 2026-07-06: P3V keeps provider activation, SDK import, key loading, key reads, env value reads, API calls, LLM calls, network calls, `call_model`, and live smoke at zero. Actual first call remains deferred to P3W or a later explicit approval phase.
- 2026-07-06: Direction decision recorded in `AICO_DIRECTION_DECISION.md`. AICO is an AI work operating harness, not a vibe-coding skill pack or generic multi-agent starter.
- 2026-07-06: Direction identity is now explicit: AICO is not a system for calling more AI; AICO is a system for deciding when AI is allowed to be called.
- 2026-07-06: Future AICO structures should be phased as `mission_interview`, `skill_registry`, `policy_pack`, `acceptance_ladder`, and `ponytail_audit`, with policy as explicit data and no hidden hooks.
- 2026-07-06: After P3V completion review, the next meaningful direction is P3W controlled single-call live smoke, not another broad no-call document phase. P3W still requires separate explicit approval and must be exactly one provider/model/key_slot/worker call with retry/reserve/fallback/second-call disabled.
- 2026-07-06: P3W implemented a controlled single-call live smoke runner, provider/model/key_slot selection validation, one-key-slot loading boundary, selected-provider SDK import boundary, single-call counter enforcement, toy mission validation, masked summary creation, and P3W artifact writers.
- 2026-07-06: P3W remains provider boundary proof only, not worker orchestration. Worker pool dispatch, manager/auditor full run, worker file writes, worker shell, web, repo clone, GitHub integration, parallel execution, retry, reserve, fallback, second call, semantic preflight, and repair loops remain blocked.
- 2026-07-06: P3W entrypoint was executed once and stopped before provider call because required P3W opt-in values were missing. Result was `blocked` / `HUMAN_DECISION_REQUIRED`, with actual_provider_call_count=0, call_model_count=0, model_call_count=0, retry_count=0, reserve/fallback/second_call=false, raw_output_saved=false, and artifact safety scan pass.
- 2026-07-06: P3W generated safe blocked artifacts under ignored run directory `runs/p3w_20260706T120953Z`: `call_attempt_summary.json`, `live_smoke_result.json`, `artifact_safety_report.json`, and `final_live_gate_result.json`. Raw key/env/output/provider response/token usage dump was not stored.
- 2026-07-06: P3W opt-in actual execution was attempted with the existing runner. Required opt-in values were not present in the process environment, so the runner stopped before provider call with `blocked` / `HUMAN_DECISION_REQUIRED`.
- 2026-07-06: The P3W opt-in attempt did not guess provider/model/key_slot, did not load `.env`, did not dump env values, did not read a key, and did not call the provider. Counts remained actual_provider_call_count=0, call_model_count=0, model_call_count=0, retry_count=0, reserve/fallback/second_call=false, raw_output_saved=false.
- 2026-07-06: P3W opt-in attempt artifacts under `runs/p3w_20260706T122055Z` passed artifact safety scan and raw leak JSON checks. The next actual one-call proof requires explicit process env opt-in values and one configured non-reserve key slot.
